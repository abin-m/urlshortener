global:
  scrape_interval: 5s
  evaluation_interval: 5s

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]

rule_files:
  - /etc/prometheus/alerts-dev.yml

scrape_configs:
  - job_name: urlshortener
    static_configs:
      - targets: ["app:8000"]
    metrics_path: /metrics
